public class Node2 {
    //let's say each node has a data
    char data;
    //to keep the node that we have already visited, create a boolean within Node class
    boolean visited;

    Node2(char data) {
        this.data = data;
    } 
}
